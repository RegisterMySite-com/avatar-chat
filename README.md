# Avatar Chat

Real-time multi-user chat built on **Cloudflare Workers**, **Durable Objects**, and **PartyServer**, with:

- Circular **image avatars** (DiceBear placeholders you can replace with your own logos)
- Public room, private invite rooms, and a direct “Message Admin” room
- Live presence
- Browser notifications + soft chime
- Light / dark theme
- Full **admin moderation dashboard** (view rooms, participants, delete messages/rooms)

Based on Cloudflare’s [durable-chat-template](https://github.com/cloudflare/templates/tree/main/durable-chat-template), extended with image avatars and an admin API.

**Repository:** https://github.com/RegisterMySite-com/avatar-chat

---

## Deploy with one click

[![Deploy to Cloudflare](https://deploy.workers.cloudflare.com/button)](https://deploy.workers.cloudflare.com/?url=https://github.com/RegisterMySite-com/avatar-chat)

After the button finishes:

1. Open the Cloudflare dashboard → **Workers & Pages** → your new Worker.
2. Go to **Settings** → **Variables** → **Add** → type **Secret**:
   - Name: `ADMIN_SECRET`
   - Value: a strong password (used for `/dashboard` login)
3. Redeploy if prompted so the secret is available.

Admin dashboard: `https://<your-worker>.workers.dev/dashboard`

---

## Deploy from the CLI (detailed)

### Prerequisites

| Requirement | Notes |
|-------------|--------|
| [Cloudflare account](https://dash.cloudflare.com/sign-up) | Free plan is enough |
| [Node.js](https://nodejs.org/) 18+ | 20+ recommended |
| [Git](https://git-scm.com/) | To clone the repo |
| Terminal | macOS / Linux / WSL / Windows Terminal |

You will also need to log in to Wrangler once:

```bash
npx wrangler login
```

This opens a browser window so Wrangler can obtain an API token for your account.

### 1. Clone this repository

```bash
git clone https://github.com/RegisterMySite-com/avatar-chat.git
cd avatar-chat
```

### 2. Install dependencies

```bash
npm install
```

Installs React, PartyServer / PartySocket, nanoid, esbuild, TypeScript, and Wrangler.

### 3. Generate Worker types (optional but recommended)

```bash
npm run cf-typegen
```

Writes/updates `src/server/worker-configuration.d.ts` so TypeScript knows about your Durable Object and asset bindings.

### 4. Local development

```bash
npm run dev
```

Wrangler starts a local server (usually `http://127.0.0.1:8787`). Open it in a browser, pick a name + circular avatar, and open the same URL in another tab or device to chat.

Tips while developing:

- The client is rebuilt by the `build.command` in `wrangler.json` on each deploy / dev start.
- Change avatars in `src/shared.ts` (`AVATARS` array). Use any square image URL or path under `public/`.
- Default admin password locally is `admin` unless you set `ADMIN_SECRET` in `.dev.vars`.

### 5. Set the admin secret

**Local (optional):** create `.dev.vars` (never commit it):

```bash
echo 'ADMIN_SECRET=your-strong-password-here' > .dev.vars
```

**Production** — set the secret on the Worker (before or right after the first deploy):

```bash
npx wrangler secret put ADMIN_SECRET
# paste your password when prompted, then press Enter
```

Or in the dashboard: **Workers & Pages** → your Worker → **Settings** → **Variables** → **Add** → type **Secret**.

### 6. Deploy

```bash
npx wrangler deploy
```

Or:

```bash
npm run deploy
```

**Important:** Use a **new Worker name** if you previously deployed an older version that used a different Durable Object class (e.g. `ChatRoom`). This template exports `Chat` and `RoomRegistry` only.

```bash
# Example: force a clean Worker name
npx wrangler deploy --name avatar-chat
```

What the first deploy does:

1. Runs esbuild (`src/client/index.tsx` → `public/dist/`)
2. Uploads static assets from `public/`
3. Creates the Worker and the Durable Object classes `Chat` + `RoomRegistry` (see `migrations` in `wrangler.json`)

You will get a URL like:

```
https://avatar-chat.<your-subdomain>.workers.dev
```

### 7. Verify

1. Open the public URL → choose a display name and a circular avatar → join **Public Room**.
2. Open the same room in another browser / incognito window and send messages.
3. Visit `/dashboard`, sign in with the `ADMIN_SECRET` you set, and confirm you can see rooms and delete messages.

### 8. Custom domain (optional)

In the Cloudflare dashboard:

**Workers & Pages** → your Worker → **Settings** → **Domains & Routes** → **Add** → attach a custom domain or route.

---

## Project structure

```
avatar-chat/
├── public/
│   ├── index.html          # SPA shell
│   ├── styles.css          # Light/dark UI + circular avatar styles
│   └── dist/               # Built client (generated by esbuild; gitignored)
├── src/
│   ├── shared.ts           # Types, AVATARS list, room constants
│   ├── client/
│   │   ├── index.tsx       # Chat UI, profile, rooms, presence
│   │   ├── AdminDashboard.tsx
│   │   └── tsconfig.json
│   └── server/
│       ├── index.ts        # Chat DO, RoomRegistry DO, admin API, Worker entry
│       ├── tsconfig.json
│       └── worker-configuration.d.ts
├── package.json
├── wrangler.json
├── tsconfig.json
└── README.md
```

### Key Durable Objects

| Class           | Purpose                                      |
|-----------------|----------------------------------------------|
| `Chat`          | One instance per chat room (messages + presence via WebSockets) |
| `RoomRegistry`  | Global list of rooms for the admin dashboard |

### Admin API (requires `Authorization: Bearer <ADMIN_SECRET>`)

| Method | Path | Description |
|--------|------|-------------|
| POST   | `/api/admin/login` | Validate password, returns token |
| GET    | `/api/admin/rooms` | List rooms + live participant counts |
| GET    | `/api/admin/rooms/:id/messages` | Messages in a room |
| DELETE | `/api/admin/rooms/:id/messages/:msgId` | Delete one message |
| DELETE | `/api/admin/rooms/:id` | Clear room + unregister |

---

## Replacing the placeholder avatars

Edit `src/shared.ts`:

```ts
export const AVATARS = [
  "/avatars/logo-01.png",
  "/avatars/logo-02.png",
  // or full CDN URLs
];
```

Put image files in `public/avatars/` (create the folder). Prefer square images; CSS crops them to circles with `border-radius: 50%` and `object-fit: cover`.

After changing client code, redeploy (`npx wrangler deploy`) so esbuild rebuilds `public/dist/`.

---

## Troubleshooting

### “New version of script does not export class 'ChatRoom'…”

An older deployment used a different Durable Object class name. This repo exports **`Chat`** and **`RoomRegistry`** only.

**Preferred fix:** deploy under a **new Worker name** (or delete the old Worker) so migrations start clean:

```bash
npx wrangler deploy --name avatar-chat
```

If you must keep the same name and the old class is no longer needed, add a migration in `wrangler.json`:

```json
"migrations": [
  {
    "tag": "v1",
    "new_sqlite_classes": ["Chat", "RoomRegistry"]
  },
  {
    "tag": "v2",
    "deleted_classes": ["ChatRoom"]
  }
]
```

Then deploy again. Only delete classes when you are sure no important data remains in those objects.

### Admin login fails

Confirm `ADMIN_SECRET` is set on the **same** Worker you are hitting, and that you redeployed (or used `wrangler secret put`) after adding it.

### Avatars show as broken images

- DiceBear URLs need network access from the browser (they are external SVGs).
- For offline / custom logos, switch `AVATARS` to paths under `public/` and redeploy.

### Client changes not visible

The browser may cache `public/dist/index.js`. Hard-refresh (Ctrl/Cmd+Shift+R) or open a private window after deploy.

---

## Scripts

| Command | Description |
|---------|-------------|
| `npm run dev` | Local development with Wrangler |
| `npm run deploy` | Build client + deploy Worker |
| `npm run cf-typegen` | Regenerate `worker-configuration.d.ts` |
| `npm run check` | Typecheck client + server + dry-run deploy |

---

## License

MIT (same spirit as the upstream Cloudflare templates).
